**模式判断**
- 这是 `kind: app` + `app.mode: workflow` 的参数抽取 workflow，主链路是 `start -> parameter-extractor -> end`。

**节点清单**
- `start_node`
- `extractor_node`
- `end_node`

**连边清单**
- `start_node -> extractor_node`
- `extractor_node -> end_node`

**字段检查表**
- `reasoning_mode` 显式为 `function_call`
- `parameters` 定义了 `city / budget`
- `city` 和 `budget` 的输出 selector 都指向 `extractor_node`

**风险分级**
- 阻塞项: 无
- 高风险项: 无
- 中风险项: 未做独立复核；参数抽取结果未做运行期成功率验证
- 优化项: 可补 instruction 提高抽取约束

**最终结论**
- `需要人工确认`

原因：静态结构正确，但没有独立复核和实体/导入器级验证。
