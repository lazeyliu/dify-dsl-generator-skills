**模式判断**
- 这是 `kind: app` + `app.mode: workflow` 的显式条件分支 workflow，主链路是 `start -> if-else -> end`。

**节点清单**
- `start_node`
- `if_node`
- `end_true`
- `end_false`

**连边清单**
- `start_node -> if_node`
- `if_node(true) -> end_true`
- `if_node(false) -> end_false`

**字段检查表**
- `cases[0].case_id = "true"`，与 `sourceHandle: "true"` 一致
- false 分支显式存在
- 两个终点都能从入口到达
- `route` 输出都能追溯到 `start_node.query`

**风险分级**
- 阻塞项: 无
- 高风险项: 无
- 中风险项: 未做独立复核与真实导入器校验
- 优化项: 可补更明确的默认分支业务说明

**最终结论**
- `需要人工确认`

原因：当前结构闭环成立，但未做独立复核，也未做真实实体/导入器级校验。
