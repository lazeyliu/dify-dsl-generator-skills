**当前任务分类**

- 这是 Dify DSL 新建任务，但当前仍处在需求未收敛阶段，主问题是澄清未知项，不是直接写 DSL。

**权限边界**

- 当前只允许收敛方案和路由，不允许直接落盘，也不应该直接输出 YAML 正文。

**主路由**

- 主路由是 `dify-dsl-brainstorming`。

**为什么不是其他 skill**

- 不是 `dify-dsl-authoring`：认证、重试、异常 JSON 路径都还是阻塞未知项。
- 不是 `dify-dsl-review`：当前不是只读审查已有 DSL。
- 不是 `dify-dsl-refactor`：当前没有现成 DSL 需要修改。

**预计联动的底座 skill**

- `dify-dsl-foundations`
- `dify-dsl-nodes`
- `dify-dsl-templates`
