使用 $dify-dsl-authoring 执行这次 forward-test。

工作目录: skills/dify-dsl-authoring/tests/cases/agent-authoring
需求说明: 用户输入 query 后交给 agent 策略执行，最终把 agent 的文本结果输出到 end。
参考样本: tests/fixtures/dsl/agent-tool-workflow.yml

这是一个真实 DSL 生成任务，不是问答。
约束：不要修改文件；先给模式判断、模板选择、节点清单、连边清单和字段检查表，再给 DSL 草稿。
如果需求仍有阻塞未知项，明确写出并停止进入正文。
