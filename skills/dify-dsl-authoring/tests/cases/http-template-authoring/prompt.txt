使用 $dify-dsl-authoring 执行这次 forward-test。

工作目录: skills/dify-dsl-authoring/tests/cases/http-template-authoring
需求说明: 用户输入 URL，系统请求该地址，解析返回的 JSON，并输出 status_code 和 parsed_data。
参考样本: tests/fixtures/dsl/template-http-workflow.yml

这是一个真实 DSL 生成任务，不是问答。
约束：不要修改文件；先给模式判断、模板选择、节点清单、连边清单和字段检查表，再给 DSL 草稿。
如果需求仍有阻塞未知项，明确写出并停止进入正文。
