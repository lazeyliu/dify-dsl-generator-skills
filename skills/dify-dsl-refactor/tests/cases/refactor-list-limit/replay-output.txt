**问题归因**
- 当前 list-operator 会保留 2 个元素，如果业务只需要 Top 1，limit 过宽。

**最小修复路径**
- 保持排序逻辑不变
- 只把 `limit.size` 从 `2` 改为 `1`

**变更影响**
- 下游只会收到一个元素
- 输出更聚焦，但信息量更少

**修改后的 DSL**
```yaml
limit:
  enabled: true
  size: 1
```

**剩余风险**
- 当前未做独立复核
- 如下游仍期待 2 个元素，需要同步调整
