**当前主任务类别**

- 已有 DSL 的只读独立复核。

**本轮复核模式**

- `异步并行`

**实际启用的复核角色**

- `字段约束复核器`
- `链路闭环复核器`
- `提示词与输入输出约定复核器`

**主要问题摘要**

- 字段约束复核器：`end_node.outputs[0].value_selector` 指向不存在的 `missing_node`。
- 链路闭环复核器：唯一边 `broken-edge` 的目标节点不存在，主路径不闭合。
- 提示词与输入输出约定复核器：当前样本没有提示词节点，但输出约定已经失真，不能给出可导入结论。

**冲突点与仲裁**

- 本轮三方结论没有实质冲突，不启用 `上线检查复核器`。

**最终统一结论**

- `明显不完整`

**剩余风险与退化说明**

- 已完成 3 个只读子代理复核。
